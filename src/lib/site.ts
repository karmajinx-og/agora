/** Canonical site URL for meta tags, JSON-LD, and sitemap. */
export const SITE_ORIGIN = 'https://agoralinux.app'

/** Default Open Graph / Twitter image (see `static/og.png`). */
export const OG_IMAGE_URL = `${SITE_ORIGIN}/og.png`

/** Categories linked from the main nav (sitemap + IA). */
export const NAV_CATEGORIES = ['Office', 'Development', 'Graphics', 'Network'] as const
